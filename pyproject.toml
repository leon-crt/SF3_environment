[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["SF3_environment"]

[project]
name = "SF3_environment"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "pre-commit",
  "numpy",
  "pathlib",
]
